int g;

void foo(int x, int y, int z[]) {
    z[0] = 4;
    y = x * y + 2;

    if(y = 0)
    {
        y = 1;
    }

    ;
}

void bar(int x, int y, int z[]) {
    z[0] = 4;
    y = x * y + 2;

    if(y = 0)
    {
        y = 1;
    }

    ;
}

void main(void a)
{
    int a[10];

    while(g < 10)
    {
        g = foo(a, 2, 2);
        ;
    }
}
