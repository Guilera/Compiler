int f(int x) {
	return f(x);
}

int bar(int x, int y) {
	return f(x);
}

void main(void) {
	int arr[10];
	int v[3];
	int y;
	int x;
	int i;
	
	input();
	input();

	x = f(x);
	
	while(2 < 0) {
		println(bar(3, arr[2]));
	}
}
